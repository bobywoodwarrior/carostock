{% extends "ValentinStockBundle:Default:_layout.html.twig" %}

{% block main %}

    <!--
    {% for product in products %}
        <p>{{ product.id }} | {{ product.name }} | {{ product.price }}€ | Quantité : {{ product.number }}</p>
    {% endfor %}
    -->

    <div class="container">
        <h2>Base de données</h2>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>id</th>
                    <th>Nom</th>
                    <th>Prix</th>
                    <th>Quantité</th>
                    <th>Total</th>
                 </tr>
            </thead>
            <tbody>
                {% for product in products %}
                    <tr>
                        <td><p>{{ product.id }}</p></td>
                        <td><p>{{ product.name }}</p></td>
                        <td><p>{{ product.price }}€</p></td>
                        <td><p>{{ product.number }}</p></td>
                        <td><p>{{ product.price * product.number }}€</p></td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <hr>

    <a href="{{ path('product_add') }}" class="btn btn-primary">Ajouter un produit</a>
    <a href="{{ path('product_edit') }}" class="btn btn-primary">Editer un produit</a>
    <a href="{{ path('product_delete') }}" class="btn btn-danger" style="float: right;">Supprimer un produit</a>
{% endblock %}
